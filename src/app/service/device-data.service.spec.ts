/* tslint:disable:no-unused-variable */
/*
import { TestBed, async, inject } from '@angular/core/testing';
import { DeviceDataService } from './device-data.service';
import {Devices} from './devices';
import {Http} from '@angular/http';
describe('DeviceDataService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      
      providers: [DeviceDataService]
    });
  });

  it('should ...', inject([DeviceDataService], (service: DeviceDataService) => {
    expect(service).toBeTruthy();
  }));

/*
describe('#getAlldevices()', () => {

    it('should return an empty array by default', inject([DeviceDataService], (service: DeviceDataService) => {
      expect(service.getAllTodos()).toEqual([]);
    }));

    it('should return all todos', inject([DeviceDataService], (service: DeviceDataService) => {
      let device1 = new Devices({title: 'Hello 1', complete: false});
      let device2 = new Devices({title: 'Hello 2', complete: true});
      service.addTodo(device1);
      service.addTodo(device2);
      expect(service.getAllTodos()).toEqual([device1, device2]);
    }));

  })

});
*/